// YYYY-MM-DD date format
export type DateOnlyString = `${number}-${number}-${number}`;

// ISO 8601 date format
export type DateISOString = `${number}-${number}-${number}T${number}:${number}:${number}Z`;
